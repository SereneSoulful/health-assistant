<Window x:Class="健康助手.Windows.ReminderPopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="健康助手提醒"
        Width="400" SizeToContent="Height"
        WindowStartupLocation="Manual"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize"
        Topmost="True" ShowInTaskbar="False"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        FontFamily="{StaticResource PixelFont}"
        TextOptions.TextFormattingMode="Display">
    <Grid x:Name="Root" Margin="16">
        <Border x:Name="Shadow" Margin="4,4,0,0" Background="{StaticResource BorderDark}"/>
        <Border Background="{StaticResource PaperTexture}"
                BorderBrush="{StaticResource BorderDark}" BorderThickness="3"
                Margin="0,0,4,4">
            <Grid Margin="14">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="NameText" FontSize="17" Foreground="{StaticResource InkBrush}"
                               VerticalAlignment="Center"/>
                    <Button Grid.Column="1" Content="×" Click="CloseButton_Click" Width="28" Height="28"
                            FontSize="14" Style="{StaticResource PixelButtonStyle}" ToolTip="已处理，重新计时"/>
                </Grid>
                <TextBlock x:Name="MessageText" Grid.Row="1" FontSize="16"
                           Foreground="{StaticResource InkBrush}" TextWrapping="Wrap"
                           Margin="0,10,0,0" MaxWidth="330"/>
                <Grid Grid.Row="2" Margin="0,14,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="点击按钮后重新计时" FontSize="12"
                               Foreground="{StaticResource InkMutedBrush}" VerticalAlignment="Center"/>
                    <Button x:Name="ActionButton" Grid.Column="1" Click="ActionButton_Click"
                            MinWidth="130" FontSize="16"
                            Style="{StaticResource PixelButtonStyle}"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
