<Window x:Class="健康助手.Windows.CountdownWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="休息倒计时"
        Width="460" Height="320"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize"
        Topmost="True" ShowInTaskbar="False"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        FontFamily="{StaticResource PixelFont}"
        TextOptions.TextFormattingMode="Display">
    <Grid Margin="16">
        <Border Margin="4,4,0,0" Background="{StaticResource BorderDark}"/>
        <Border Background="{StaticResource PaperTexture}"
                BorderBrush="{StaticResource BorderDark}" BorderThickness="3"
                Margin="0,0,4,4">
            <Grid Margin="18">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock x:Name="MessageText" FontSize="17" Foreground="{StaticResource InkBrush}"
                           TextWrapping="Wrap" TextAlignment="Center"/>
                <Grid Grid.Row="1">
                    <TextBlock x:Name="RemainingText" Text="20" FontSize="88"
                               Foreground="{StaticResource AccentBrush}"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
                <UniformGrid x:Name="ProgressGrid" Grid.Row="2" Rows="1" Columns="20"
                             Height="14" Margin="0,0,0,16"/>
                <Grid Grid.Row="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="休息一下，让眼睛放松" FontSize="12"
                               Foreground="{StaticResource InkMutedBrush}" VerticalAlignment="Center"/>
                    <Button x:Name="SkipButton" Grid.Column="1" Content="跳过" Width="100"
                            FontSize="14" Style="{StaticResource PixelButtonStyle}"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
